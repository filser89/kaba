<!--
  research-log.md entry template — /kaba:research appends ONE such entry per run to
  FEATURE_DIR/research-log.md (append-only: never overwrite earlier entries; separate
  entries with a --- rule). Read by a human deciding an open question.
  Keep it short: Basis, Recommendation + rationale, Rejected Alternatives. No investigation transcript.
  Replace every [bracketed] placeholder. Remove this comment in the output.
-->
# Research: [question]

**Date**: [today's date]
**Basis**: [what was consulted — e.g. "web search + Gemfile (links in Rejected Alternatives)" or "stack knowledge + Gemfile; no search (mature domain)"]

## Recommendation

[The single recommended decision, stated concretely — e.g. "Generate PDFs with Prawn."]

[One paragraph of rationale, grounded in THIS project's stack and constraints — referencing maturity/community where relevant.]

## Rejected Alternatives

- **[Option]** ([link if available]) — [one line: why it lost for this stack]
- **[Option]** ([link if available]) — [one line: why it lost; e.g. "too new, abandonment risk"]
